# Phase 2, Products 1–10 — Media Map

Which asset file to use for each of the first 10 products in `collection-expansion.md`. All assets live in `assets/motifs/` (default colorway: Signature — swap to `--inverse` or `--warm-stone` filenames for other colorways).

| # | Product | Motif form | Asset file (Signature) |
|---|---|---|---|
| 1 | Tea towel | Hairline border + single-sprig corner | `hairline-border--signature.png` (edge) + `single-sprig--signature.png` (corner) |
| 2 | Apron | Single-sprig emblem + hairline hem border | `single-sprig--signature.png` (chest/pocket) + `hairline-border--signature.png` (hem) |
| 3 | Oven glove | Hairline border | `hairline-border--signature.png` |
| 4 | Pot holder | Hairline border | `hairline-border--signature.png` |
| 5 | Placemat (set of 4) | Dense border | `dense-field--signature.png` |
| 6 | Coaster set | Single-sprig emblem | `single-sprig--signature.png` |
| 7 | Chopping board | Hairline border | `hairline-border--signature.png` |
| 8 | Serving tray | Dense field center + hairline edge | `dense-field--signature.png` (center) + `hairline-border--signature.png` (edge) |
| 9 | Storage tin | Single-sprig emblem | `single-sprig--signature.png` |
| 10 | Mug | Single-sprig emblem | `single-sprig--signature.png` |

All 10 products are covered by the same 3 base motif files — no product-specific artwork is needed beyond cropping/scaling/positioning within each product's actual Contrado template. Products flagged with two assets (1, 2, 8) combine a placement emblem with a border/edge treatment.

Next step per product: open its real template in Contrado's design tool, confirm print-area dimensions and bleed, then position the relevant asset(s) — scale up using the `.svg` source if the PNG resolution isn't sufficient for that product's canvas.
